#include "monty.h"
/**
 * nop - Doesn't do anything
 *
 * @stack: The stack
 * @line_number: line number from which element is
 *
 * Return: Nothing
 */
void nop(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
	/* No operation, do nothing */
}
